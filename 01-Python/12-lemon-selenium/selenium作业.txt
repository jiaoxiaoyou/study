1， selenium 是采用什么网络协议实现的框架？ 简述 selenium 原理
2，在百度首页搜索柠檬班，进入柠檬班腾讯课堂，并定位其中的某位老师。
3，自己写一个 html 页面，并定位其中的元素


 打开课堂派，通过 xpath 表达式定位元素
- 通过 f12 的 ctrl + f 输入表达式，确保元素唯一；
- 写出页面、元素和对应的表达式
- 写出 py 文件

1， 写一个 html 页面，包含 iframe 和 select， 通过 python 执行下列操作：
2, 定位 iframe 中的元素
3, 选择 select 中的元素
4，使用显示等待

根据上课内容，完成以下功能的实现：
1，发送键盘
2，将任意网页窗口移动到最底部
3，修改 12306 日期控件
4，上传文件